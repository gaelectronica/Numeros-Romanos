function iniciar()
{
	cajadatos = document.getElementById('cajadatos');
	var boton = document.getEventListener('boton');
	boton.addEventListener('')
}

window.addEventListener('load', iniciar, false);